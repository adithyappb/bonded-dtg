/**
 * Spark — Bonded’s native reward & utility token (concept).
 * Accrues from verified dates, reputation, and matchmaker activity. Use it alongside
 * USDC stakes, native ETH, and Bitcoin-side flows; it does not replace those assets.
 */
export const SPARK_TICKER = "SPARK" as const;
export const SPARK_NAME = "Spark";
export const SPARK_SYMBOL = "✦";

/** Demo starting balance (synced across wallet / profile / dashboard copy). */
export const DEMO_SPARK_BALANCE = 2450;

export const SPARK_TAGLINE =
  "Bonded’s app-native token for rewards and perks — stacks with your other crypto, not instead of it.";
